<h1>Editing store</h1>

<% semantic_form_for(@store) do |f| %>

  <%= f.error_messages %>

  <% f.inputs do %>
    <%= f.input :location %>
    <%= f.input :manager, :as => :select, :collection => @store_managers %>
  <% end %>
    <%= f.buttons %>
<% end %>

<%= link_to 'Show', @store %> |
<%= link_to 'Back', stores_path %>